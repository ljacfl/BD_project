version: '3'
services:
  mongodb:
    image: mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: JACF
      MONGO_INITDB_ROOT_PASSWORD: 123
    volumes:
      - ./data/db:/data/db

  redis:
    image: redis
    ports:
      - "6379:6379"